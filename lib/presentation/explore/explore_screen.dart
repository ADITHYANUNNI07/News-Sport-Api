import 'package:flutter/material.dart';

class ExploreScrn extends StatelessWidget {
  const ExploreScrn({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Explore')),
    );
  }
}
